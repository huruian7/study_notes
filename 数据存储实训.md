# 第一章 前导篇
### 1.1 JSP
```text
JSP既写java的业务逻辑，又写网页，后端又当爹又当妈（前后端耦合度太高已被淘汰，但需要基本掌握使用流程）

现在企业级开发一般都是前后端分离的模式，用json文件(类似于python中的字典)进行传输
```

```text
一，JSP知识点讲解

1）

```
### 1.2 SSM框架和Vue.js框架
```text
SSM分为SpringMVC，Spring，Mybatis三个基础框架（后端）
Vue.js框架（前端），".js"表示框架的生态是JavaScript



PS:在企业开发中一般采用SSM优化版框架SpringBoot
```

### 1.3 MVC模式
```text
MVC是一种软件架构设计模式,分为Model，View，Controller

Model:项目的核心，包含各种业务逻辑以及和数据库交互的实体
View:负责展示的页面，比如用Vue框架写的前端页面
Controller:负责接收用户的点击或输入，判断用户请求，是联系Model和View的桥梁
```

### 1.4 Tomcat

1.5 传统jdbc连接MySQL和使用Tomcat

主机和虚拟主机？

### 1.5 服务器种类
```text
纯Web服务器(每个网站背后都有一个Web服务器或者服务器集群)
纯应用服务器（如学生管理系统背后的业务逻辑处理就是在应用服务器里）
数据库服务器（MySQL）
文件服务器（百度网盘，微信文件传输助手）

Tomcat是内置Web服务器的应用服务器
```
### 1.6 Servlet
```text
Servlet（服务器端小程序）是运行在Web服务器或应用服务器上的Java程序，是客户端（浏览器）和服务端（数据库）之间的中间层，从源码上可以理解为一个接口
```

# 第二章 Tomcat
## 2.1 Tomcat下载及配置
```text
Tomcat本身就是一个Java编写的程序，双击startup.bat启动时必须依赖电脑的java运行环境

Q1:初次启动会乱码，因为Tomcat默认编码UTF-8（为全球设计），而命令行默认GBK（专为中文环境）
A1:在 conf\logging.properties ,把 java.util.logging.ConsoleHandler.encoding = UTF-8 这一行的代码改为GBK
```

