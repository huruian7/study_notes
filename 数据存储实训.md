# 第一章 前导篇
### 1.1 JSP
```text
JSP既写java的业务逻辑，又写网页，后端又当爹又当妈（前后端耦合度太高已被淘汰，但需要基本掌握使用流程）

现在企业级开发一般都是前后端分离的模式，用json文件(类似于python中的字典)进行传输
```

```text
一，JSP知识点讲解

1）

```
### 1.2 SSM框架和Vue.js框架
```text
SSM分为SpringMVC，Spring，Mybatis三个基础框架（后端）
Vue.js框架（前端），".js"表示框架的生态是JavaScript



PS:在企业开发中一般采用SSM优化版框架SpringBoot
```

### 1.3 MVC模式
```text
MVC是一种软件架构设计模式,分为Model，View，Controller

Model:项目的核心，包含各种业务逻辑以及和数据库交互的实体
View:负责展示的页面，比如用Vue框架写的前端页面
Controller:负责接收用户的点击或输入，判断用户请求，是联系Model和View的桥梁
```

### 1.4 Tomcat

1.5 传统jdbc连接MySQL和使用Tomcat

主机和虚拟主机？

### 1.5 服务器种类
```text
1.纯Web服务器(每个网站背后都有一个Web服务器或者服务器集群)
2.纯应用服务器（如学生管理系统背后的业务逻辑处理就是在应用服务器里）
3.数据库服务器（MySQL）
4.文件服务器（百度网盘，微信文件传输助手）
PS：Tomcat是内置Web服务器的应用服务器（服务器不一定是真实存在的计算机，还可以是软件层面上的）

Q：如何理解服务器这个相关概念?
A:在计算机领域，服务器分为软件层面和硬件层面。当我们用自己的计算机去点击startup.bat启动时Tomcat这个程序就充当了Web服务器，负责监听8080端口，只要有浏览器通过这个端口发送http请求，Tomcat就会接收并发送请求。而此时我们自己的计算机充当了应用服务器，ta提供cpu去运算Tomcat的代码，提供内存去缓存网页数据，提供网卡去接收网络请求。
```
### 1.6 Servlet
```text
Servlet（服务器端小程序）是运行在Web服务器或应用服务器上的Java程序，是客户端（浏览器）和服务端（数据库）之间的中间层，从源码上可以理解为一个接口
```

### 1.7 Maven
```text
Maven是java的包管理工具，IDEA有内置Maven，但一般建议自己手动配置

手动配置需要更改Maven repository且更改仓库镜像
```

# 第二章 Tomcat
## 2.1 Tomcat运行配置
```text
Tomcat本身就是一个普通的Java编写的程序，双击startup.bat启动时必须依赖电脑的java运行环境。在双击startup.bat程序后，电脑就会出现一个cmd界面（java程序），会在本地计算机开启特定端口（如8080）

Q1:初次启动会乱码，因为Tomcat默认编码UTF-8（为全球设计），而命令行默认GBK（专为中文环境）
A1:在 conf\logging.properties ,把 java.util.logging.ConsoleHandler.encoding = UTF-8 这一行的代码改为GBK
```

## 2.2 IDEA连接Tomcat
### 2.2.1 连接步骤
```text
第一步，下载Smart Tomcat插件
  由于IDEA社区版不提供web相关服务，需下载插件。如果IDEA网络波动下载不了，可以去官网下载插件包再从磁盘安装到IDEA中
  
第二步，创建Maven Web项目
Archetype选择org.apache.maven.archetypes:maven-archetype-webapp，创建好后Maven会从互联网下载各种该模版所需要的jar包和配置文件

第三步，配置pom.xml
pom代表项目对象模型，Maven通过读取这个文件来了解项目的结构，需要哪些jar包
由于普通的 Java 环境里没有处理网页请求的“ Servlet 类”，所以我们需要在pom.xml声明<dependency>让Maven自动下载

第四步，修改运行配置为Tomcat
由于smart tomcat是独立的运行环境，所以如果控制台乱码，可以在运行配置中的VM option中添加-Dfile.encoding=GBK
当我们在运行 Java 程序时，其实是先启动了一个JVM，然后由虚拟机来跑你的代码。VM Options 就是在虚拟机启动的一瞬间，传给它的“启动指令”。
```

### 2.2.2 pom.xml介绍
pom(Project Object Model)项目对象模型
书写jar包的位置，Maven管理jar包（jar包是java的程序包，jar包之间会相互依赖）
```text
1）在Maven的世界里，每个项目都有一个唯一编号，由以下部分组成：
  Groupid（域名反写，代表组织）
  Artifactid(项目名称)
  version(版本号)
2）dependencies
  依赖管理,告诉Maven下载哪些依赖
3）packaging
  告诉Maven，这个项目的成品不是.jar，而是一个.war包（专门给Tomcat运行的）
```


delete from（保留索引）
truncate  （清空索引）
