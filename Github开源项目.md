# AList+IDM-Activation-Script无限续杯
1. AList负责获取直链地址，idm负责下载
2. IDM-Activation-Script负责清空本机idm的注册信息（右击运行脚本文件即可）
```
idm设置：
	用户代理设置为netdisk     //让网盘以为idm的请求是自家客户端发出来的，减少拦截
	默认最大连接数改为32      //提升速度

如果idm过期了，重新以管理员身份运行IAS（汉化）.cmd
```
```
Alist设置：
	
```