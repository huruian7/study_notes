# 第一章 SQL语句
DDL，DML，DQL，DCL
alt table student    add （column）添加字段，位置补充控制，first，after（没有）	    
				modify    修改字段
				 change   重命名字段且可以修改字段属性（相当于自己重新创建一个字段）
				 drop       删除字段
	注意：均可以多行，比如add s_age int,add s_name varchar(50);modify,change,drop同理



