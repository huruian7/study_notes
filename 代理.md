发明git和代理的真的是天才!!!
怪不得说学计算机容易进去
linux is very beautiful

squid:一台长期运行在 Linux 上的中间转发程序

## 【第一步：在 香港服务器 上执行】

### 1. 安装 squid 代理
```shell
apt update && apt install squid
```
### 2. 备份原配置（好习惯）
```shell
cp /etc/squid/squid.conf /etc/squid/squid.conf.bak
- #bak is bakeup
```
### 3. 3. 写入安全配置
```bash
# 监听 3128 端口 
http_port 3128 
# 允许你的IP访问（注意：这里要写本地公网IP） 
acl my_home src 117.136.110.154
http_access allow my_home 
# 拒绝所有其他访问 
http_access deny all 
# 缓存相关配置 (注意64MB值和单位之间需要有空格)
cache_mem 64 MB maximum_object_size 2048 MB 
# DNS 服务器（谷歌+Cloudflare） 
dns_nameservers 8.8.8.8 1.1.1.1
```
### 4. 重启并开机自启
```bash
sudo systemctl daemon-reload 
sudo systemctl restart squid 
sudo systemctl enable squid
```
### 5. 放行防火墙 3128 端口
```bash
sudo ufw allow 3128/tcp
- #ufw,Ubuntu 系统默认的防火墙工具
```