
超文本标记语言
超文本：突破了普通文本的限制
标记语言：由标签名构成

浏览器内核对前端代码进行解析，进而渲染出界面

# 第一章 HTML
## 1.1 标签
### 1.1.1 基本框架
```html

```

表格跨行跨列

表单
label的for和id绑定（焦点绑定）
用name属性分组（比如单选框性别，男和女归为一组，可以只选其一）
type(表单的类型)
required

label和input关联方式有两种
第一种为嵌套关联
第二种为for-id关联

# 第二章 CSS

## 2.1 引用方式
```text
内联样式  单个元素
内部样式  当前页面
外部样式  整个项目

优先级：内联>内部>外部
```
## 2.2  基础选择器
### 2.2.1 标签选择器
```text
如标签<p></p>
``` 
### 2.2.2 类选择器
```text
通过HTML的class属性匹配，支持多类名
```
### 2.2.3 id选择器
```text
通过HTML的id属性匹配，支持多id
```
### 2.2.4 通用选择器
```text
*，给所有标签修改样式
```
## 2.3 复合选择器
由两个或多个基础选择器复合在一起
### 2.3.1 后台选择器
```tex
空格

```
### 2.3.2 子代选择器
```text

```