
超文本标记语言
超文本：突破了普通文本的限制
标记语言：由标签名构成

浏览器内核对前端代码进行解析，进而渲染出界面

# 第一章 HTML
## 1.1 标签
### 1.1.1 基本框架
```html

```

表格跨行跨列

表单
label的for和id绑定（焦点绑定）
用name属性分组（比如单选框性别，男和女归为一组，可以只选其一）
type(表单的类型)
required

label和input关联方式有两种
第一种为嵌套关联
第二种为for-id关联

# 第二章 CSS

## 2.1 引用方式
```text
内联样式  单个元素
内部样式  当前页面
外部样式  整个项目

优先级：内联>内部>外部
```
## 2.2  基础选择器
### 2.2.1 标签选择器
```text
如标签<p></p>
``` 
### 2.2.2 类选择器
```text
通过HTML的class属性匹配，支持多类名
```
### 2.2.3 id选择器
```text
通过HTML的id属性匹配，支持多id
```
### 2.2.4 通用选择器
```text
*，给所有标签修改样式
```
## 2.3 复合选择器
由两个或多个基础选择器复合在一起(<span style="color: #ff0000;">包含标签要注意层级，比如p不能包含div</span>)
### 2.3.1 后代选择器
```tex
空格
选择某元素匹配的所有后代元素
```
### 2.3.2 子代选择器
```text
> 大于号
仅选择某元素的直接子元素
```
### 2.3.4 并集选择器
```text
, 逗号
```
### 2.3.4 伪类选择器
```shell
给元素的某种状态/位置添加样式
1,a:link(鼠标未点击)
2,a:visited（鼠标已点击）
3,a:hover（鼠标悬浮，常用）
4,a:active（正在点击的那一瞬间）
5,a:focus(输入框/按钮所选状态)

:first-child
:nth-child(even)
:not(:last-child)
```
## 2.4 盒子模型

## 2.5 响应式布局

伪元素：不存在的标签
：： 虚拟元素，无真实DOM节点

伪类：不改变标签本身，只改变ta的某种状态/位置
::before 装饰提示文本
content设置为空
display

::after 清除浮动