## 第一章 HBase
要求掌握HBase基础概念
- HBase是一种分布式的NoSQL数据库，存储数据稀疏，多维
- Hadoop是框架
包含HDFS（分布式文件系统），YARN（资源调度管理器），MapReduce（分布式计算模型）三个核心组件
- HBase物理存储结构，kv键值对存储
一个表切片为多个Region，分布在不同服务器上，再按照列族（一个列族对应一个store）拆分为多个store，用多个HFile文件进行存储
- key=RowKey+列族+列名+时间戳（复合键）
- 为什么一个store会有多个HFile？
HDFS是按key有序读取，写数据先写到内存中（一般为128MB），内存满了就会往硬盘里写，一个store如果很大很大，就会刷写出多个HFile

## 第二章 OpenClaw
SSH端口转发
在本地电脑和远程服务器挖了一条隧道，把远程服务器的某个服务搬到本机电脑前(正是因为这个隧道，所以我们可以访问openclaw控制台)
```shell
ssh -N -L 18789:127.0.0.1:18789 ubuntu@43.133.185..65
```